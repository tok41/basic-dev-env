version: '3'

services:
    tok-base:
        build:
            context: .
            dockerfile: ./docker/Dockerfile
        ports:
            # 公開用のポート, host:container
            - "8066:8888"
        volumes:
            # host:container
            - $PWD:/workspace
        tty: true
